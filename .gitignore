.aws-sam/
__pycache__/
*.pyc
**/.DS_Store
project/scripts/*.zip

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
# asdf tool pins are local dev preferences, not committed (lock files are).
**/.tool-versions
terraform/modules/lambda/.placeholder_*.zip

# Pytest cache
.pytest_cache/
