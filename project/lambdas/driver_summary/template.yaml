AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: driver_summary — lap statistics for a driver from RDS

Globals:
  Function:
    Runtime: python3.9
    Timeout: 30
    MemorySize: 256
  HttpApi:
    CorsConfiguration:
      AllowMethods:
        - GET
        - OPTIONS
      AllowHeaders:
        - Content-Type
      AllowOrigins:
        - "*"

Parameters:
  DbHost:
    Type: String
    Default: localhost
  DbPort:
    Type: String
    Default: "5432"
  DbName:
    Type: String
    Default: racetrack
  DbUser:
    Type: String
    Default: racetrack
  DbPassword:
    Type: String
    NoEcho: true
    Default: racetrack

Resources:
  DriverSummaryFunction:
    Type: AWS::Serverless::Function
    Properties:
      Handler: handler.handler
      CodeUri: .
      Environment:
        Variables:
          DB_HOST: !Ref DbHost
          DB_PORT: !Ref DbPort
          DB_NAME: !Ref DbName
          DB_USER: !Ref DbUser
          DB_PASSWORD: !Ref DbPassword
      Events:
        DriverSummary:
          Type: HttpApi
          Properties:
            Path: /driver-summary
            Method: GET

Outputs:
  ApiEndpoint:
    Description: "Base URL — use GET /driver-summary?session_key=...&driver_number=..."
    Value: !GetAtt ServerlessHttpApi.ApiEndpoint
  DriverSummaryFunctionArn:
    Description: ARN of the Lambda function
    Value: !GetAtt DriverSummaryFunction.Arn
